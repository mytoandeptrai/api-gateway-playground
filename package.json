{
  "name": "turbo-app-template",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency=13 --filter=!api",
    "start": "turbo run start --concurrency=13 --filter=!api",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "dev:services": "turbo run dev --concurrency=15 --filter=api-gateway --filter=orchestrator-service --filter=auth-service --filter=order-service --filter=inventory-service --filter=payment-service --filter=refund-service --filter=shipping-service --filter=notification-service --filter=product-service",
    "dev:web": "turbo run dev --filter=web",
    "dev:fresh": "bash scripts/dev-fresh.sh",
    "only-infra": "cd docker && docker compose -f docker-compose-infra.yml up -d",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d --build",
    "docker:down": "docker compose -f docker/docker-compose.yml down",
    "docker:down:v": "docker compose -f docker/docker-compose.yml down -v"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.8",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=18"
  }
}