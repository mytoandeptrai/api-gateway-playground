interface:
  display_name: "Api Gateway Playground"
  short_description: "Repo-specific patterns and workflows for api-gateway-playground"
  default_prompt: "Use the api-gateway-playground repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true